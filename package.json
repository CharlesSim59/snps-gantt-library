{
  "name": "snps-gantt-library",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm -w @snps/gantt-core run test",
    "typecheck": "tsc -b packages/core packages/react apps/demo",
    "dev": "npm -w demo run dev",
    "build": "npm -w demo run build"
  },
  "devDependencies": {
    "typescript": "~5.5.4",
    "vitest": "^2.1.0"
  }
}
